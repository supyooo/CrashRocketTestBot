# Crash Rocket

Telegram Mini App crash game (front-end prototype). The round, players and balance are simulated in the browser for now.

- `app/` — the Mini App, deployed to GitHub Pages on every push to `main`.
